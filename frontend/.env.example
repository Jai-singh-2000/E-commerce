# Base URL for API calls. Leave empty in development so requests go through the
# Vite dev proxy configured in vite.config.js.
VITE_PUBLIC_URL=

# Where the dev proxy forwards /api requests.
VITE_API_PROXY_TARGET=http://localhost:8000

# Dev server port.
VITE_PORT=5173
